export default async function SettingsPage() {
    return (
        <div>
            Select a user to start a conversation
            change password
            delete account
            share unique id
        </div>
    )
}
